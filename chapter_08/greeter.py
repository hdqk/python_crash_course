def greet_user(username):  # defines a function called greet_user
    """Display a simple greeting."""
    print(f"Hello, {username.title()}!")


greet_user('hector')
#'hector' is an "argument"
#username is a "parameter"